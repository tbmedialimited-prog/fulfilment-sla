v5-limit-fix
Built: 2026-05-25
Changes from v4:
  - getAllOrders now uses sql(queryString) function form instead of tagged template
    (Neon serverless driver has issues parameterizing LIMIT in tagged templates)
  - Default limit reduced from 5000 to 2000 across all callers
  - safeLimit inlined directly into SQL (validated as integer first)

This should fix the "/api/raw returns 0 orders" issue.
