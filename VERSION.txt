v10-dpd-bearer
Built: 2026-05-25
Changes from v9:
  - /api/dpd-debug now tests BOTH auth methods:
    * Bearer token (modern developers.api.dpd.co.uk style)
    * X-API-Key header
    * Legacy Basic/Session (api.dpd.co.uk username/password)
  - Tests against 3 base URLs: api.dpd.co.uk, api.dpdlocal.co.uk, developers.api.dpd.co.uk
  - Tests 5 candidate paths per base = 15 bearer attempts + 2 X-API-Key + 2 Basic logins
  - Total 19 attempts in one call, shows which combo works

ENV VAR NEEDED:
  DPD_API_KEY (set this in Vercel to your portal API key)
