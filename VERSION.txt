v11-dpd-correct-base
Built: 2026-05-25
Changes from v10:
  - DPD base URL back to api.dpdlocal.co.uk (confirmed working by diagnostic)
  - DPD tracking endpoint: /shipping/shipment/?searchCriteria=<tracking>
    (was using wrong _search endpoint before)
  - DPD search response parser handles multiple JSON shapes
  - /api/dpd-debug v3 now does end-to-end test: login -> search -> tracking detail
    and shows you the actual response shape so we can fine-tune field extraction

NEXT STEP:
  1. Visit /api/dpd-debug?tracking=15976913071805
  2. Should see step1_login.ok=true, step2_search with found shipment,
     step3_tracking with delivery events
  3. Paste the response - we'll see if data flows
